require 'spec_helper'

describe EdimaxDhcpStatus do
  it 'has a version number' do
    expect(EdimaxDhcpStatus::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
